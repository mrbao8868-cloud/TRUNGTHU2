export interface Category {
  id: string;
  name: string;
  prompt: string;
  icon: React.ReactNode;
}

export interface AspectRatio {
  id: string;
  name: string;
  promptSuffix: string;
  icon: React.ReactNode;
}
